apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

labels:
  - includeSelectors: true
    pairs:
      env: dev

patches:
  - path: replica-count.yaml
    target:
      kind: Deployment
      name: webapp-dev
